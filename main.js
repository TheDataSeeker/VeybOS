// Not sure if this is required but I tend to include it anyway.
const fs = require("fs");
const MKD = require("mkdcolor");

function Show(name) {
  switch(name) {

    default: 
      putLang("error_noNamedFound");
      break;
  }
}